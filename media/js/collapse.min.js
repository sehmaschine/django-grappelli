/*
(function($) {
    $(document).ready(function() {
        $('fieldset.collapse').each(function() {
            $(this).find('h2:first').addClass("collapse-handler");
        });
        $('.collapse-handler').bind('click', function(){
            if (!$('body').hasClass('collapse-blocked')) {
                var collapsible = $(this).parent();
                var collapsibleExclude = '.collapse-handler, .module .tools';
                
                // Simple Toggle Behaviour
                collapsible.toggleClass('open').toggleClass('closed');
            };
        });
    });
})(jQuery);
*/